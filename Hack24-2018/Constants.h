#pragma once

const float WINDOW_SIZE = 1000;
const float STAR_SIZE = 4;
const int NUMBER_OF_ARMS = 6;
const int planetDistanceMin = 40;
const int planetDistanceMax = planetDistanceMin + 40;

const sf::Vector2f CENTRE(WINDOW_SIZE / 2.f, WINDOW_SIZE / 2.f);

